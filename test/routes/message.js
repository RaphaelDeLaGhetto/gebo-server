'use strict';

var message = require('../../routes/message');

/**
 * sendMessageHandler
 */
exports.sendMessageHandler = {

    'It should do something': function(test) {
        test.done();
    }
};
